# default layout (can be bsp, stack or float)
yabai -m config layout bsp

# yabai -m config status_bar   off

yabai -m config auto_balance on


# addins shadow to windows
#yabai -m config window_shadow on
#yabai -m config window_border on
#yabai -m config window_border_width 20
#yabai -m config active_window_border_color 0x00059c32
#yabai -m config normal_window_border_color 0x000a4fbf
#yabai -m config insert_window_border_color 0xffd75f5f

# New window spawns to the right if vertical split, or bottom if horizontal split
yabai -m config window_placement second_child

# padding set to 12px
yabai -m config top_padding 8
yabai -m config bottom_padding 8
yabai -m config left_padding 8
yabai -m config right_padding 8
yabai -m config window_gap 8

# modifier for clicking and dragging with mouse
yabai -m config mouse_modifier alt
# set modifier + left-click drag to move window
yabai -m config mouse_action1 move
# set modifier + right-click drag to resize window
yabai -m config mouse_action2 resize

# when window is dropped in center of another window, swap them (on edges it will split it)
# yabai -m mouse_drop_action swap

# disable yabai control on this window
yabai -m rule --add app="^System Settings$" manage=off
yabai -m rule --add app="^Raycast$" manage=off



# config for sketchybar
#yabai -m config external_bar all:32:0

# border
borders active_color=0xff4deeea inactive_color=0xff494d64 hidpi=on width=5.0 &


